import React from 'react';
import { connect } from 'react-redux';
import {Grid, Row, Col, Image, Panel } from 'react-bootstrap';

const Post = ({title, postMemory, postPhoto, user}) => (
  <div>
    This is a Post Page
  </div>
);
export default Post;
