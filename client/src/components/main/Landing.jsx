import React, { Component } from 'react';
import { bindActionCreators } from 'redux';
import { connect } from 'react-redux';

const Landing = () => {
  return (
    <div>
      This is a bogus landing page.
    </div>
  );
};

export default Landing;